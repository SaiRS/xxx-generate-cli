// 为了让ts编译后的目录中有package.json文件，因此
import packageJson from '../package.json';
packageJson;

export default function init(): void {
  // eslint-disable-next-line no-console
  console.log('init 00000000000000000');
}
